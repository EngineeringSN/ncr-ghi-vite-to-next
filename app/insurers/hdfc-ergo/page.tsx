import { seoMeta } from "@/lib/seo-meta";
import HDFCErgo from "../../../components/pages/insurers/HDFCErgo";
export const metadata = seoMeta.hdfcErgo;
const page = () => {
  return <HDFCErgo />;
};

export default page;
