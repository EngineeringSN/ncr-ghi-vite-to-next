import TermsConditions from '@/components/pages/TermsConditions';
import { seoMeta } from '@/lib/seo-meta';
import React from 'react'
export const metadata = seoMeta.TermsConditions;

const page = () => {
  return <TermsConditions/>
}

export default page